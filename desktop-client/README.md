# desktop-client

Tauri desktop shell for sealClaw.

Planned responsibilities:
- native window and tray
- auto update
- local Python runtime lifecycle management
- calls to `user-center` and local `agent-runtime`

Suggested next steps:

```bash
npm install
npm run tauri:dev
```
